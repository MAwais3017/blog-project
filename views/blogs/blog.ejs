<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title><%= blog.title %></title>
  </head>
  <body>
    <div class="header">
      <h1><%= blog.title %></h1>
    </div>
    <div class="container">
      <p><%= blog.content %></p>
      <p>Posted by <%= blog.author %> on <%= blog.created_at %></p>

      <!-- Like Blog -->
      <form action="/blogs/<%= blog.id %>/like" method="POST">
        <button type="submit">Like</button>
      </form>
      <p>Likes: <%= blog.likes %></p>

      <!-- Comment Section -->
      <h3>Comments</h3>
      <ul>
        <% comments.forEach((comment) => { %>
        <li><%= comment.content %> - <%= comment.author %> on <%= comment.created_at %></li>
        <% }) %>
      </ul>

      <form action="/blogs/<%= blog.id %>/comment" method="POST">
        <textarea name="content" rows="4" placeholder="Add your comment"></textarea>
        <button type="submit">Submit</button>
      </form>
    </div>
  </body>
</html>
